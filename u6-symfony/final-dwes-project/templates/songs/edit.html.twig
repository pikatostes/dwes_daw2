{% extends 'base.html.twig' %}

{% block title %}Edit Songs{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h1 class="mb-4">Edit Songs</h1>

        <div class="card">
            <div class="card-body">
                {{ include('songs/_form.html.twig', {'button_label': 'Update'}) }}

                <hr class="my-4">

                <div class="container d-flex justify-content-around">
                    <a href="{{ path('app_songs_index') }}" class="btn btn-secondary mr-2">Back to List</a>
                    {{ include('songs/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

