{% extends 'base.html.twig' %}

{% block title %}Playlists index{% endblock %}

{% block body %}
    <h1>Playlists index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for playlist in playlists %}
            <tr>
                <td>{{ playlist.id }}</td>
                <td>{{ playlist.Name }}</td>
                <td>
                    <a href="{{ path('app_playlists_show', {'id': playlist.id}) }}">show</a>
                    <a href="{{ path('app_playlists_edit', {'id': playlist.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_playlists_new') }}">Create new</a>
{% endblock %}
